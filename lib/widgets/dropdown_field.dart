import 'package:flutter/material.dart';

class DropDownFiled extends StatelessWidget {
   
  const DropDownFiled({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('DropDownFiled'),
      ),
    );
  }
}